export interface Fruit {
    name: string
    unitPrice: number
    image: string
}

export interface Cart {
    customerName: string
    address: string
    delivery: string
    fruits: Fruit[]
}

export interface Cart {
    customerName: string
    address: string
    delivery: string
    fruits: Fruit[]
}